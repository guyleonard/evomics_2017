- name: A bunch of aliasses I like to include
  copy: src:"{{playbook_dir}}/files/bash_aliases" dest=/home/genomics/.bash_aliases
  become: true
  become_user: genomics 

- name: Change bash PS1 promt colour etc
  copy: src="{{playbook_dir}}/files/bash_profile" dest=/home/genomics/.bash_profile
  become: true
  become_user: genomics
