- name: Install ETE3 from Conda
  shell: "/home/{{workshop_user}}/miniconda3/bin/conda install -c etetoolkit ete3 ete3_external_apps"
  become: true
  become_user: "{{workshop_user}}"
  when: ete3_env_installed.stat.exists == false
